import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Files in /public are served at the root URL (e.g. /PersonalWebsite.png).
  // The `images` option only affects `next/image`, not plain <img> tags.
};

export default nextConfig;
